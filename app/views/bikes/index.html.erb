<div class="title">
  <h1>Welcome to HaBike</h1>
  <h3>The place to find your next ride</h3>
</div>
<%= link_to "Sell your bike", new_bike_path(@bike) %>
<div class="container">
  <% @bikes.each do |bike| %>
    <%= link_to bike_path(bike) do %>
    <div class="bike-card">
      <% if bike.photo.attached? %>
        <%= cl_image_tag bike.photo.key, class: "bike-image" %>
      <% else %>
        <%= image_tag "bike.jpg", class: "bike-image" %>
      <% end %>

      <div class="bike-details">
        <h3><%= bike.model %></h3>
        <p><%= bike.year %> &middot; <%= bike.kilometers %> km &middot; <%= bike.category %></p>
        <div class="bike-price">
          <p class="price">Precio contado</p>
          <p class="price-amount">$<%= bike.price %></p>
        </div>
      </div>
    </div>
    <% end %>
  <% end %>
</div>
